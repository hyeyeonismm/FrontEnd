import { useNavigate } from 'react-router-dom';
import { Grid, Img, Button } from '../components';

function DetailWaste() {
  const navigate = useNavigate();

  return (
    <>
      <h2>DetailWaste</h2>
    </>
  );
}
export default DetailWaste;